{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build_libft_test",
            "type": "shell",
            "command": "C:/msys64/mingw64/bin/gcc.exe",
            "args": [
                "-g",
                "${workspaceFolder}/test.c",
                "-L${workspaceFolder}",
                "-lft",
				"-g",
                "-o",
                "${workspaceFolder}/test.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Compiles test.c with libft.a"
        }
    ]
}